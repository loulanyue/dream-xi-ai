{
  "name": "@dream-xi/queue",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — In-process task queue with concurrency control, priority scheduling, delay execution, and per-task retry. Zero dependencies.",
  "keywords": ["dream-xi", "queue", "task-queue", "concurrency", "priority", "scheduler", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
