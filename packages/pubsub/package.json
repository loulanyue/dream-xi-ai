{
  "name": "@dream-xi/pubsub",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — In-process pub/sub message bus with wildcard topics, async dispatch, once-listeners, and message filtering. Zero dependencies.",
  "keywords": ["dream-xi", "pubsub", "event-bus", "publish-subscribe", "wildcard", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
