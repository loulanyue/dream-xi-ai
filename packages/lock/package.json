{
  "name": "@dream-xi/lock",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — In-process async mutex and semaphore for preventing concurrent race conditions in Agent workflows. Zero dependencies.",
  "keywords": ["dream-xi", "lock", "mutex", "semaphore", "concurrency", "async-lock", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
