{
  "name": "@dream-xi/throttle",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Execution throttling and debouncing utilities for limiting Player action frequencies. Zero dependencies.",
  "keywords": ["dream-xi", "throttle", "debounce", "rate-limiting", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
