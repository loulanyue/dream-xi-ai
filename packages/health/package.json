{
  "name": "@dream-xi/health",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Deep health check system: liveness, readiness, startup probes with dependency checks and structured JSON reports. Zero dependencies.",
  "keywords": ["dream-xi", "health", "liveness", "readiness", "probe", "kubernetes", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
