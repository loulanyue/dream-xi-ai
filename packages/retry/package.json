{
  "name": "@dream-xi/retry",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Production-grade retry utility: exponential backoff, jitter, circuit breaker, and retry budgets. Zero dependencies.",
  "keywords": ["dream-xi", "retry", "backoff", "circuit-breaker", "resilience", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
