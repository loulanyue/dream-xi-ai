{
  "name": "@dream-xi/context",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — LLM conversation context manager with token budget control, sliding window, message compression, and snapshot/restore. Zero dependencies.",
  "keywords": [
    "dream-xi",
    "context",
    "llm",
    "conversation",
    "token-budget",
    "sliding-window",
    "typescript"
  ],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
