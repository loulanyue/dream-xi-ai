{
  "name": "@dream-xi/rate-limiter",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Token bucket & sliding window rate limiter for API endpoint protection. Zero dependencies.",
  "keywords": ["dream-xi", "rate-limiter", "throttle", "token-bucket", "sliding-window", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
