{
  "name": "@dream-xi/state",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Finite State Machine (FSM) implementation for managing structured Agent workflow state transitions. Zero dependencies.",
  "keywords": ["dream-xi", "state", "fsm", "state-machine", "workflow", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
