{
  "name": "@dream-xi/hook",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Lifecycle hook system for Agent players: register, invoke and manage ordered async callbacks for player lifecycle events. Zero dependencies.",
  "keywords": ["dream-xi", "hook", "lifecycle", "agent-hooks", "event-hooks", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
