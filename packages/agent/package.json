{
  "name": "@dream-xi/agent",
  "version": "0.1.0-alpha",
  "description": "Dream XI AI — Base Agent model binding identity, ContextWindow, state, and event publishing. Zero dependencies.",
  "keywords": ["dream-xi", "agent", "agent-runner", "state", "typescript"],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@dream-xi/context": "workspace:*",
    "@dream-xi/pubsub": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
